import React from 'react';
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const FillCredentials = () => {
  return (
    <SafeAreaView>
      <Text>FillCredentials</Text>
    </SafeAreaView>
  );
};

export default FillCredentials;
